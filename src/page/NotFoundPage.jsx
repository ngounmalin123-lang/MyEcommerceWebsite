import NotFound from "../components/NotFound";

const NotFoundPage = () => {
    return (
        <div className="text-center py-20">
            <NotFound></NotFound>
        </div>
    );
};

export default NotFoundPage;